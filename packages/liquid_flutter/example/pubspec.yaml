name: flutter_liquid_example
description: Feature catalog example for flutter_liquid.
publish_to: none

environment:
  sdk: ^3.10.9

dependencies:
  flutter:
    sdk: flutter
  flutter_liquid:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
